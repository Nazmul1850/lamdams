# ── Core scientific stack ──────────────────────────────────────────────────────
numpy
scipy
matplotlib
pandas
sympy
tqdm

# ── Qiskit (circuit generation & transpilation) ────────────────────────────────
# qiskit 2.x is already installed; no old sub-packages (terra/aer/ignis) needed
qiskit

# ── Constraint solver (CP-SAT scheduler) ──────────────────────────────────────
ortools

# ── Graph libraries ────────────────────────────────────────────────────────────
igraph
rustworkx   # replaces old retworkx

# ── ZX-calculus (used by lsqecc) ──────────────────────────────────────────────
pyzx

# ── Lattice-surgery compiler (lsqecc + lsqecc_slicer CLI) ────────────────────
# IMPORTANT: install with --no-deps on Python 3.13 — lsqecc lists qiskit-terra
# as a dependency but that package has no Python 3.13 wheels and fails to build.
# qiskit-terra was merged into qiskit 1.0+; qiskit 2.x already satisfies the
# actual runtime imports. Command:
#   pip install --no-deps "git+https://github.com/latticesurgery-com/lattice-surgery-compiler.git"
# lsqecc @ git+https://github.com/latticesurgery-com/lattice-surgery-compiler.git

# ── Misc utilities ────────────────────────────────────────────────────────────
dill
texttable
immutabledict
ply
typing_extensions
